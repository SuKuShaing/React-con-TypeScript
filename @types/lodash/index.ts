declare module "lodash" { // el nombre del modulo debe ser exactamente igual al nombre del modulo que se está importando
    export function random(lower: number, upper: number): number; // exporta la función random, definimos lo que ingresa y sale de la función
}